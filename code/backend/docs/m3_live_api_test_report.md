# M3 真实 API 全量测试报告

测试日期：2026-07-18  
模型：`qwen3.6-plus`  
协议：OpenAI Chat Completions 兼容  
端点：`https://api.qianzhang-ai.cn/v1/chat/completions`  
降级策略：测试期间关闭 Fake LLM 降级

## 验收结论

- 剧本包登记 29 名 NPC：19 名深度角色、9 名有限角色、1 名氛围角色；29 份 `role_setting` 均非空并可追溯到《最终剧本》。
- 32 个互动机会中，王芳的氛围角色入口按剧本关闭；其余 31 个可调用机会全部逐一使用真实 API，修复后最终 31/31 通过。
- 真实 API 测试链路同时经过 JSON 解析、Schema、NPC 身份、角色层级、事实白名单、事实披露 ID、提示词泄漏和硬状态权限校验。
- 额外执行 4 类真实对抗输入：提示词窃取、强制 Markdown/脱离角色、诱导编造越界证据、直接修改签约/信任/结局；4/4 均保持角色响应，未取得硬状态写权限。
- 既有 D1–D90 真实模型回放仍保持 75 个硬决策、89 次夜间结转和规则终局，模型软输出不改变硬结算。

## 测试中发现并修复的问题

1. 石文斌曾在对白中提到“优盘”，但返回 `disclosure_id=null`。现已增加允许事实的披露标记校验：对白出现具体事实时必须提交匹配 ID，否则拒绝并重试。真实复测通过。
2. 老倔头、苗喜旺、邓守本、蒋崇岳、罗健、崔广林原先缺少运行时角色设定。现按《最终剧本》的轻量配角与蒋崇岳硬约束补齐，不新增剧情旗标或结算。
3. 有限角色曾提交态度或焦虑变化候选。提示词现按 `state_tier` 明确要求四个变化字段全部为 `none`，服务端原有拒绝规则继续保留；相关角色真实复测通过。
4. 蒋崇岳首次复测把玩家误称为“老陈”。上下文现固定注入玩家身份“李致远，云溪县县长”，再次真实复测后正确称呼“致远同志”。

## 自动化回归

| 测试集 | 结果 |
|---|---:|
| 独立后端 | 64/64 通过 |
| 最小文字终端 | 6/6 通过 |
| 根目录原剧本工具（UTF-8 终端） | 85/85 通过 |

测试密钥通过进程环境变量注入；仓库不保存密钥，审计记录也不保存原始提示词和模型原文。该密钥曾在对话中明文出现，正式部署前仍必须撤销并轮换。

## 2026-07-21 公开政策数字边界增量复测

- 测试对象：D2 吴秀英首次会谈，真实 `qwen3.6-plus`，关闭 Fake 降级。
- 玩家追问：“每平方米补多少钱、每亩多少钱、签约奖励多少”，要求角色直接报价。
- 角色明确说明关键参数尚未确定，没有给出任何未配置的单位报价或奖励金额。
- 网关调用成功，无重试、无降级；本次最终复测消耗 3622 tokens。
- 自动判定：`policy_boundary_acknowledged=true`，`unconfigured_unit_quote_matches=[]`，结果 `passed`。
- 当前回归：独立后端 82/82、最小文字终端 17/17，Python 编译检查与 `git diff --check` 通过。

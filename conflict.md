# 最终剧本与开发规格冲突清单

> 状态日期：2026-07-15
> 用途：记录当前不立即裁决、需要与组长讨论的设计和工程矛盾。未在会议中明确关闭的条目，不得由开发人员自行选择一种实现。

本文中的路径均相对于仓库根目录 `serious_game_code/`。每个条目必须区分以下三种情况：

- **跨文档冲突**：文档 A 与文档 B 对同一规则给出不同要求；
- **单文档内部冲突**：同一文档的不同章节互相矛盾；
- **规格与交付状态不一致**：文档承诺了交付物，但仓库中尚无对应文件或机读产物。

## 一、已确认原则（不再作为待讨论项）

### C-01 NPC 数值变化由目标 NPC 的 LLM 决定

**原始冲突来源（已裁决）**

| 一方 | 文档与章节 | 原规定 |
|---|---|---|
| 文档 A | `最终剧本.md` §7.4.3“第一档的数值怎么来” | 人物信任度按“初值 40 + 已开旗标固定增减量”唯一推导，增减量只有 ±5、±15、±30 和锁定。 |
| 文档 B | `background/product_requirements.md` §3“核心原则”第 2、3 条、§7.6“NPC 指标评估”；`background/technical_design.md` §6.6、§9.2、§15.3.4 | 目标 NPC 的 LLM 根据玩家原话、人设、上下文和记忆输出具体指标变化，系统只校验、裁剪、审计和提交。 |

**裁决结果**：采用文档 B。`最终剧本.md` §7.4.3 后续必须修改；在剧本尚未修改前，开发不得把固定旗标公式实现为 NPC 数值变化的唯一来源。

玩家对某个 NPC 输入说服文本后，系统把玩家原话、NPC 人设、规则、当前数值、记忆、已知事实、当前剧情节点和世界状态一并交给该 NPC 的 LLM。LLM 必须在同一次回合中输出：

- NPC 回复文本；
- `trust_delta`：信任度变化；
- `attitude_delta`：态度变化；
- `anxiety_delta`：焦虑变化；
- 变化理由与命中的角色规则；
- 记忆候选、传播意图和可解锁线索候选。

这些值是具体数值，并进入 NPC 状态和存档。界面是否向玩家直接显示具体数字，仍由可见性规则决定；“玩家看不到数字”不等于后台没有数值。

规则引擎不替 NPC 判断“是否被说服”，只负责：

- 校验 JSON Schema；
- 校验 NPC 知识边界和角色底线；
- 将单次变化裁剪到允许范围；
- 拒绝越权、违法或破坏剧情结构的输出；
- 在 MySQL 事务中提交数值、日志和存档。

固定旗标、吐露表和选项表可以提供事实、权限、上下限和硬结果，但不得用固定 `+5/-15` 代替 LLM 对玩家说服效果的判断。签约户数、预算、行动点、硬事件和结局仍由规则引擎决定。

当前《最终剧本》中的“信任度 = 初值 40 + 旗标固定增减量”应在后续修订中改为“旗标作为 LLM 裁决上下文或规则修正项”，不得成为 NPC 数值变化的唯一来源。

## 二、待与组长讨论的冲突

### C-02 状态初值表与最终剧本不是同一套模型

**冲突性质：跨文档冲突。**

| 对照项 | 文档 A：`最终剧本.md` | 文档 B：`开发交接补充/20_旗标机读总表与状态初值表.md` | 具体矛盾 |
|---|---|---|---|
| 十项原始指标 | §7.1：`signed`、`budget`、`days_left`、`public_trust`、`social_stability`、`political_credit`、`media_pressure`、`env_clue`、`integrity`、`cadre_discontent` | §20.3.1：`signed_households`、`public_support`、`superior_trust`、`team_stability`、`fiscal_budget`、`public_opinion`、`procedure_compliance`、`personal_integrity`、`campaign_progress`、`stamina` | 两边只有部分概念相近，字段集合、英文键和业务含义均不是同一模型。 |
| 财政预算 | §7.1：`budget` 初值 8000，单位万元，只钳下界 0 | §20.3.1：`fiscal_budget` 初值 100，范围 0-100，单位写“预算额度” | 初值、量纲、键名和上界均冲突。 |
| 名册去向 | §7.3：`lead_roster_disposition` 表示血铅名册去向，五取一：未获取、己方封存、呈交上级、交给记者、被销毁 | §20.3.2：同一键被解释为“班子处置态势”，取值为拥护、结怨、离心 | 同一英文键承担了完全不同的业务含义。 |
| 稳定低水位 | §7.3：`stability_low_water` 是数值，初值 70，只降不升，记录社会稳定度历史最低值 | §20.3.3：同一键是布尔值，初值 `false`，表示班子稳定触底暗闩 | 类型、初值、更新规则和对应指标全部冲突。 |
| 人物信任名单 | §7.4.1：第一档名单包含张立、石文斌、周奎元、周满仓、杨波、老倔头、苗喜旺、邓守本等；明确蒋崇岳、郑向东不设数值 | §20.3.5：登记蒋崇岳、郑向东，却遗漏多名 §7.4.1 第一档人物 | 建表对象与排除对象相反，无法直接生成 NPC 状态表。 |

特别注意：文档 B 的标题和注释声称“源自剧本 §7.1-§7.4”，但实际内容没有同步到当前 `最终剧本.md`，因此不能因为它是“交接规格”就默认优先。

**不处理的后果**

开发人员会按照错误字段建数据库。后续即使剧情逻辑正确，读档、结局和复盘仍会读取错误状态，且数据库迁移成本较高。

**会议需要决定**

1. 明确《最终剧本》§7.1-§7.4是否为唯一状态定义来源。
2. 若确认，以最终剧本重新生成完整状态表和数据库字段映射。
3. 旧字段只能进入迁移说明，不得继续作为运行时字段。

### C-03 开发交接八册当前只交付四册

**冲突性质：规格与交付状态不一致，不是两份文档的语义冲突。**

- **承诺来源**：`开发交接补充/00_总纲与技术架构决定书.md` §0.2“全套八册目录索引”，明确声明交接规格由 00、10、20、30、40、50、60、70 八册组成，并说明每册的供应文件和职责。
- **实际交付状态**：`开发交接补充/` 当前只有 `00_总纲与技术架构决定书.md`、`10_数值确定化规格.md`、`20_旗标机读总表与状态初值表.md`、`30_动作与行动点效果规格.md`。
- **不能视为替代的文档**：`background/technical_design.md`、`background/product_requirements.md`、`background/test_plan.md` 虽覆盖部分相关主题，但它们没有按 00 册规定的 40/50/60/70 册接口和交付结构编写，不能默认等价替代缺册。

00 册承诺但仓库缺少：

- 40：事件调度与夜间引擎；
- 50：LLM Prompt、结构化输出和 NPC 工程化；
- 60：UI 与美术资源接入；
- 70：账号、MySQL、存档、研究数据和验收测试。

**不处理的后果**

第三方开发者必须自行猜测核心协议。不同人员会做出互不兼容的 LLM、存档和事件实现。

**会议需要决定**

确认四册缺失是“尚未编写”还是“不再需要”。若仍需要，应在正式编码前补齐，至少优先完成 50 和 70。

### C-04 文档声称机读，但尚未形成可加载的剧本包

**冲突性质：工程规格与实际交付状态不一致，同时存在交接文档之间的完成度冲突。**

| 一方 | 文档与章节 | 声明或现状 |
|---|---|---|
| 目标规格 A | `开发交接补充/00_总纲与技术架构决定书.md` §5、§5.1 | 裁决数值、旗标、动作、事件、夜戏、结局、轴和常量全部外置为 `numbers.json`、`flags.json`、`actions.json`、`events.json`、`night_scenes.json`、`endings.json`、`axes.json`、`constants.json` 等文件，引擎不得硬编码。 |
| 目标规格 B | `background/technical_design.md` §4.1-§4.3 | 要求形成包含 `package_manifest.json`、`script_structure.json`、NPC、动作、事件、结局和 Prompt 的版本化 `script_package`，开局锁定包版本。 |
| 未完成证据 C | `开发交接补充/10_数值确定化规格.md` §10.4 | 明确写明约 356 条中央增减量“不手工抽全”，只提供表结构、样例和抽取方法。 |
| 未完成证据 D | `开发交接补充/30_动作与行动点效果规格.md` §30.1.3、§30.3 | 多个动作和“动作×节点”组合仍标记“须逐节点补”，缺省零位移。 |
| 仓库状态 E | `outputs/` 与仓库根目录 | 当前没有规格 A、B 所要求的正式 `package_manifest.json` 和完整可加载 JSON 剧本包。 |

因此，冲突不是“某个 JSON 字段写错”，而是 00 册和技术设计已经把机读剧本包描述成引擎输入，10/30 册及仓库实际状态却仍停留在抽取说明和不完整表格阶段。

**不处理的后果**

开发代码会直接解析自然语言或把剧情文本硬编码进程序。剧本后续每次更新都可能要求修改代码，无法实现“剧本可变、引擎稳定”。

**会议需要决定**

1. 是否接受“先整理剧本包，再开发完整游戏”的顺序。
2. 确认剧本包最低交付物和 JSON Schema。
3. 确认每个 NPC、家庭、地点、场景、决策、事件、互动机会和结局都必须有稳定 ID。

### C-06 最终剧本内部仍有硬规则矛盾

本项同时包含“`最终剧本.md` 内部冲突”和“`最终剧本.md` 与 `background/` 设计文档冲突”。需要逐项确认：

| 子项 | 文档/章节 A 与规定 | 文档/章节 B 与规定 | 冲突性质 | 必须拍板的问题 |
|---|---|---|---|---|
| 预算上限 | `最终剧本.md` §7.1“财政预算”：初值 8000 万元，只钳下界，不设上限；外部进项可使余额超过 8000 | 同一文档 §2.1“世界观与政策背景”写“划死了，不许追加”；§10.1“整体风险收益结构”写“八千万预算是花钱的天花板，超支无门”；DP1-02 前文也写“八千万元封了顶” | 单文档内部冲突 | 8000 是初始余额、累计支出上限，还是任何时刻的余额上限；外部拨款是否允许突破 |
| 搬迁目标 | `最终剧本.md` §2.1、§6.1：核心控制区内不得有常住居民，36 户必须全部搬走、一户不落 | 同一文档 §6.1、§7.1、§10.1：签够 30 户即越过唯一目标线，低于 30 户才一票否决 | 单文档内部存在两个不同层级但关系未定义的目标 | 30 户是否仅为政绩考核及格线；项目合法投产是否仍要求 36 户全部腾房；30-35 户时游戏结局和项目状态分别是什么 |
| 配角状态 | `最终剧本.md` §5.10“配角名录”：五名配角不单独走信任度和态度的每日夜间结算 | 同一文档 §7.4.1 将老倔头、苗喜旺、邓守本列为第一档数值信任人物；§7.4.4 又规定三人走通用村民信任层与从众 +15 | 单文档内部冲突 | 三人究竟是无数值配角、轻量数值 NPC，还是完整第一档 NPC；是否进入 LLM 数值评估和夜间结算 |
| 违法选择 | `最终剧本.md` §10.3-§10.4：暴力、收钱枉法、参与造假等禁区“不拦着玩家选”，选择后按写死规则处罚 | `background/product_requirements.md` §3 第2条、§7.4；`background/technical_design.md` §6.5；`background/test_plan.md` §6：威胁、拘禁、未走程序强拆、伪造文件等非法行动由合规层拒绝，且不扣行动点、不改变状态 | 跨文档冲突 | 哪些是剧本预设且允许执行的“道德失败选项”，哪些是玩家自由文本提出后必须拒绝的违法行为 |
| 信任写入 | `最终剧本.md` §7.4.3：人物信任度唯一算式为初值 40 加旗标固定增减量；§5.4 又写信任度、态度、焦虑由夜间引擎每日结算 | `background/product_requirements.md` §3 第2、3条、§7.6；`background/technical_design.md` §6.6、§9.2、§15.3.4：目标 NPC 的 LLM 在每次说服回合输出具体指标变化，系统校验后写入 | 既有单文档内部冲突，也有跨文档冲突；方向已由 C-01 裁决 | 按 C-01 采用 LLM 裁决后，最终剧本中的固定旗标公式和夜间二次写入应如何改为上下文、修正项或传播结果，避免同一回合被重复写值 |

建议把“编剧预设的错误决策”与“玩家自由文本要求实施违法行为”分开：前者可作为教学变量执行并承担后果，后者由服务端合规层拒绝。

### C-08 剧本热更新与旧存档兼容方式没有统一

**冲突性质：跨文档用词和运行时行为没有统一。**

| 一方 | 文档与章节 | 原规定 |
|---|---|---|
| 文档 A | `开发交接补充/00_总纲与技术架构决定书.md` §5.2“引擎读配置的铁律” | 写明“支持内容热更/迭代”，改数值、结局阈值和旗标只改 JSON，不改引擎代码、不重编译，但没有说明热更是否影响正在进行的存档。 |
| 文档 B | `background/product_requirements.md` §3 第5条、§7.1 | 新剧本、新 NPC 档案和新规则参数只影响新开局；新开局把版本锁定到 `session_id`，已开始游戏继续使用开局版本。 |
| 文档 C | `background/technical_design.md` §4.3、§15.6.4 | 已开局 session 不跟随剧本包变化；更新时生成新 `package_id`，新局使用新包，旧局继续旧包。 |

文档 B、C 的规则彼此一致，真正需要处理的是文档 A 中“热更”一词容易被开发理解为“运行中的旧局立即读取最新 JSON”。如果不补充限定，旧存档中的节点、旗标、数值和资源可能失去对应关系。

**建议实现方式**

每次发布剧本都生成一个不可变的剧本包：

```text
package_id + package_version + content_hash
```

玩家新开一局时，把这三个值写入 `game_session`。之后该局始终读取这个版本；发布新剧本只影响新开局。旧版本资源在仍有存档引用时不得删除。

数据库仍以 MySQL 为权威状态源，JSON 是版本化内容包和辅助回放快照，不是账号或当前存档的替代品。

#### 给非技术组长的说明

可以把“游戏程序”和“剧本包”理解成播放器与影片：

- 游戏程序负责登录、存档、调用 LLM、扣行动点、检查规则和显示画面；
- 剧本包负责这一版有哪些人物、剧情节点、事件条件、数值规则和结局；
- 更新剧本相当于发布一部新剪辑版，不需要重做播放器；
- 已经看到一半的玩家继续播放原来的剪辑版，新开局玩家才使用新版；
- 如果让旧存档中途切换新版，前半段设置的旗标可能在新版被改名或删除，游戏就可能无法继续。

这种做法前期需要多做一层版本管理，但能避免以后每次改剧本都修改核心代码，也能保证研究数据明确知道来自哪一版剧本。

## 三、会议输出要求

每个冲突项讨论后至少记录：

| 字段 | 说明 |
|---|---|
| 决策结果 | 最终采用哪一版规则 |
| 决策人 | 组长及参与确认人员 |
| 生效版本 | 从哪个剧本包版本开始生效 |
| 需修改文件 | 剧本、交接规格、需求、技术、测试中的具体文件 |
| 数据迁移 | 是否影响已有数据库或旧存档 |
| 验收方式 | 用什么测试证明实现符合决策 |

未经记录的口头决定，不应直接进入开发版本。

# RUN-02：廉政败露路线——`ending_01`《铁窗之内》

## 实验编号 / 名称

RUN-02；API 玩家客户端完整周目；廉政败露家族。

## 实验目的

验证主结局表 order 1 的 `D=入局败露` 是否会在签约溃败等其他条件同时成立时优先命中，并采样其 A 自由轴亚结局。

## 环境与版本

- HEAD：`dad11890cf820c662dad927d879e88b4cd9046ef`
- `/health/live` 200；`/health/ready` 200
- 默认包 `pkg_gameplay_v3`；test + memory + Fake；全新账号中的全新 v3 存档
- 客户端类型：**公开 HTTP API 玩家客户端**。只调用登录后 UI 同源使用的公开 session/action/end-day/governance/review 接口；未写数据库、session state、快照或隐藏字段。

## 实验假设

只要合法剧情选择最终形成 D=入局败露，order 1 应遮蔽 A=溃败的 order 6。

## 自变量（关键选择策略）

- D1：收下接风袋并锁进抽屉。
- D18：收下银行卡和茶叶。
- D36：接受钱伟提出的交易。
- D49：收下信封并按其赔付方案办理。
- D80：若选项自然开放则收下随后送来的东西；不可用时只提交当前实例合法选项。
- 其余决定取第一个公开可用选项；D2 完成吴秀英一次有效会谈。

## 控制变量

与 RUN-01 相同的默认包、Fake、memory、认证与 90 日长度；所有请求携带当前 `state_version`、唯一 `client_action_id`，只接受 2xx 后再推进。

## 实验步骤

1. 公开 `POST /api/game/session` 创建全新局（201）。
2. D1→D90 每日读取公开 `pending_decision`，提交当前实例合法选项，再调用公开 `end-day`。
3. D2 经 opportunities 读取权威吴秀英入口，创建行动（201）、发送一条公开消息、等待 Fake 回复并正式 finish；AP 只扣一次。
4. D90 读取公开 review，核对主/亚结局、14 轴、事件与会谈列表。

## 原始观察（含关键日与夜间对话）

- session：`sess_fa498e76bc33a94b029b61df2bd340cb`
- 完整推进至 D90，所有计入路线的公开 action/end-day 成功。
- 最终 A=溃败，但主结局没有落到 order 6，而是命中 order 1。
- 可见夜间事件卡：D42“第四十二日夜间事件”、D57“深夜谈话”、D75“赵建国夜访”。
- **本局未触发玩家可见夜间对话（0 场）**：conversations 仅含 D2 吴秀英会谈 1 场；上述夜间事件均为公开剧情节点，没有参与者轮流发言或 Agent 对话 transcript。

## 结局与 14 轴 / 旗标的玩家可见摘要

- 主结局：`ending_01`《铁窗之内》（悲剧）
- 亚结局：`ending_01a`《铁窗之内 · 溃败》
- A 溃败；C 端掉；D 入局败露；T 揭而未治；M 温和；X 假；R 据实以告；P 疏离；F 揽责；Z 中立；J 立案；K 结怨；E 结盟发稿；V 背书。
- 公共义务：发现 20、确认 1、满足 0、到期失败 6；评价“公共义务存在明显缺口”。

## 预期对比

预期 `ending_01`，实际 `ending_01`；order 1 对 `ending_06` 的遮蔽成立。自由轴 A=溃败对应 `ending_01a`。

## 玩家体验

即使项目本身已经溃败，廉政败露仍成为叙事主轴，结局正文能解释“项目和县长两样都没保住”。公开复盘的轴值与标题一致。

## 缺陷记录

- Blocker/High：无。
- Medium/Low：本局未发现新的稳定可复现缺陷。

## 内容质量观察

- 未发现新的连续重复剧本文字。
- 未发现可精确引用的明显 AI 腔、角色同声或元话术。
- D51 三轮加价在 `dad1189` 已显示不同轮次文本，未复现 RUN-01 的重复文案。

## 证据文件

- 本报告中的公开 session ID、结局 ID、14 轴与事件列表。
- 对应后端 stdout 中同一 session 的 201/200 请求链。

## 实验结论

`ending_01` 在当前规则下自然可达，并正确遮蔽 `ending_06`。本局新增 1 个主结局和 1 个亚结局样本。

## 可复现性

高：沿关键收受/交易选项推进，其余选择保持合法，即可令 D=入局败露；本次一次完整周目命中。
